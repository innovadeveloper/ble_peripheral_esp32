#include "./config/config.h"
#include "iostream"
#include "./libraries/button.h"

void setupButtons(
    // const std::vector<Button> &buttonList,
    const std::function<void(const Button &)> &closure)
{
    // for (size_t i = 0; i < buttonList.size(); ++i) {
    //     std::cout << "Button " << i << ": " 
    //               << "simpleClickCommand = " << buttonList[i].simpleClickCommand 
    //               << ", longClickCommand = " << buttonList[i].longClickCommand 
    //               << ", pin = " << buttonList[i].pin 
    //               << std::endl;
    // }
}
